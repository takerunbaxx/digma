    
<div class="bg-slider">
    <div class="jumbotron p-3 mb-5 rounded text-center">
        <div class="px-0">
         <h1 style="font-family: cooper black;">Digma</h1>
         <h3>どこに行きますか？</h3>
         <div class="form-group">
           <%= form_with(url: search_result_admins_path, method: :get, local: true) do |f|%>
           <%=f.select :keyword, [['北海道'], ['青森県'], ['岩手県'],['宮城県'], ['秋田県'], ['山形県'], ['福島県'], ['茨城県'], ['栃木県'],
      ['群馬県'], ['埼玉県'], ['千葉県'], ['東京都'], ['神奈川県'], ['新潟県'], ['富山県'], ['石川県'], ['福井県'], ['山梨県'], ['長野県'], ['岐阜県'],
      ['静岡県'], ['愛知県'], ['三重県'], ['滋賀県'], ['京都府'], ['大阪府'],['兵庫県'], ['奈良県'], ['和歌山県'],['鳥取県'], ['島根県'], ['岡山県'],
      ['広島県'], ['山口県'], ['徳島県'],['香川県'], ['愛媛県'], ['高知県'],['福岡県'], ['佐賀県'], ['長崎県'],['熊本県'], ['大分県'], ['宮崎県'],
      ['鹿児島県'], ['沖縄県']], prompt: "都道府県を選択してください", autofocus: true, autocomplete: "prefecture_name", class: "ef col-sm-5"%> <br>
          <i class="fas fa-search-location fa-lg"></i><%=f.submit "探してみる", class: "btn btn-info"%>
          
           <%end%>
         </div>        
         </div>
            <div class="d-flex flex-column">
               <h5 id="typing1">日本の隅々まで様々な施設の予約ができる、それが"Digma"</h5>
               <h5 id="typing2"></h5>
           </div>
     </div>

</div>
   
   
   
<script>

  $('.jumbotron').hide().fadeIn(3000);

  $('h1').hide().fadeIn(3000);
  
  $('h2').hide().fadeIn(3000);

const typing = (element, sentence) => {
  [...sentence].forEach((character, index) => {
    setTimeout(() => {
      document.querySelector(element).textContent += character;
    }, 50* ++index);
  });
}

//typing('#typing1', "日本の隅々まで様々な施設の予約ができる、それがDigma。");
//typing("#typing1", " 日本の隅々まで様々な施設の予約ができる、それが  Digma"  );
typing("#typing2", " 予約金なし、キャンセル規約一律。全てオンライン決済。Digmaで予約してみよう。");

</script>

 <script>
jQuery(function($) {
    $('.bg-slider').bgSwitcher({
        images: ['/assets/camping.jpeg','/assets/ocean.jpeg','/assets/kurashiki1.jpeg',"/assets/kurashiki2.jpeg"], // 切替背景画像を指定
    interval: 5000, // 背景画像を切り替える間隔を指定 3000=3秒
        loop: true, // 切り替えを繰り返すか指定 true=繰り返す　false=繰り返さない
        shuffle: true, // 背景画像の順番をシャッフルするか指定 true=する　false=しない
        effect: "drop", // エフェクトの種類をfade,blind,clip,slide,drop,hideから指定
        duration: 500, // エフェクトの時間を指定します。
        easing: "swing" // エフェクトのイージングをlinear,swingから指定
    });
});
</script>

